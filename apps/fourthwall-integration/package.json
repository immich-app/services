{
  "name": "@immich-services/fourthwall-integration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:queue": "wrangler dev --config wrangler.queue-processor.toml",
    "build": "wrangler deploy --dry-run --outdir ../../dist/fourthwall-integration && wrangler deploy --dry-run --outdir ../../dist/fourthwall-integration --config wrangler.queue-processor.toml",
    "tail": "wrangler tail",
    "test": "vitest",
    "check": "tsc --noEmit"
  }
}
