name = "blog-importer-immich-app"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# R2 bucket binding for static assets
[[r2_buckets]]
binding = "STATIC_BUCKET"
bucket_name = "immich-static"
