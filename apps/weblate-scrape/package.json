{
  "name": "@immich-services/weblate-scrape",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir ../../dist/weblate-scrape",
    "tail": "wrangler tail",
    "test": "vitest",
    "check": "tsc --noEmit"
  }
}
