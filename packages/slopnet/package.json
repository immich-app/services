{
  "name": "slopnet",
  "version": "0.1.0",
  "description": "PeerJS connection management library with automatic reconnection, heartbeat, and host/client coordination",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "peerjs": "^1.5.0"
  },
  "devDependencies": {
    "peerjs": "^1.5.4"
  }
}
